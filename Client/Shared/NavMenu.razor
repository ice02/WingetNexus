        <AuthorizeView>
            <Authorized>

                @* <li class="nav-item px-3">
                    <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                        <span class="oi oi-home" aria-hidden="true"></span> Home
                    </NavLink>
                </li>
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="directapi">
                        <span class="oi oi-list-rich" aria-hidden="true"></span> Direct API call
                    </NavLink>
                </li> *@

                <MudNavMenu>
                    <MudNavLink Href="" Match="NavLinkMatch.All" Icon="@Icons.Material.Filled.Home">Home</MudNavLink>
                    <MudNavLink Href="packagemanager" Icon="@Icons.Material.Filled.Apps"> Applications </MudNavLink>
            <MudNavLink Href="github" Icon="@Icons.Material.Filled.Gite">Import from github</MudNavLink>
            <MudNavLink Href="healthchecks" Icon="@Icons.Material.Filled.WheelchairPickup"> Health checks </MudNavLink>
            <MudNavLink Href="docs/view" Icon="@Icons.Material.Filled.DocumentScanner"> Swagger </MudNavLink>
            <MudNavLink Href="userinfos" Icon="@Icons.Material.Filled.VerifiedUser"> UserInfos </MudNavLink>
            @* <AuthorizeView Policy="IsAdmin">
                <Authorized Context="adminCtx"> *@
            <MudNavGroup Title="Admin zone" Expanded="true">
                <MudNavLink Href="tanants-admin" Icon="@Icons.Material.Filled.GroupWork"> Tenants </MudNavLink>
                <MudNavLink Href="usersadmin" Icon="@Icons.Material.Filled.People"> Users </MudNavLink>
                <MudNavLink Href="publisher-admin" Icon="@Icons.Material.Filled.Publish"> Publishers </MudNavLink>
            </MudNavGroup>
                    
                    
                @* </Authorized>
            </AuthorizeView> *@
            

                </MudNavMenu>
  
            </Authorized>
            <NotAuthorized>
                <p class="whiteColor">no access</p>
            </NotAuthorized>
        </AuthorizeView>
        

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
